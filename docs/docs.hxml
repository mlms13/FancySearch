# cleanup
-cmd rm -rf xml
-cmd rm -rf pages
-cmd mkdir -p xml

# build without output
-cp ../src
-lib thx.core
-D doc-gen
-D dox
-dce no
--no-output
ImportFancy
-xml xml/fancy.xml
-js _

# generate documentation
-cmd haxelib run dox -i xml/fancy.xml -o pages --title "Fancy"
